{
  "name": "neon-dev-portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "test": "jest",
    "test:ui": "playwright test",
    "test:api": "jest --testPathPattern=api",
    "test:all": "npm run test && npm run test:ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "jest": "^29.7.0"
  }
}
